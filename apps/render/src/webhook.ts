// Placeholder — real webhook delivery implementation lands in T5.
// Keeping the no-op signature here so main.ts compiles in T4.

export async function sendCallback(_jobId: string, _callbackUrl: string | null): Promise<void> {
  // intentionally empty
}
