# Default registry — npmmirror is the official china mirror (replaces deprecated npm.taobao.org).
# Override via env var NPM_CONFIG_REGISTRY in CI or per-developer if you're outside China.
registry=https://registry.npmmirror.com/

# Faster install by disabling progress bar in non-TTY environments
auto-install-peers=true
strict-peer-dependencies=false
