# Iter 22 — Auto-save + Module Verification

> **For agentic workers:** Use superpowers:subagent-driven-development.

**Goal:** 接上 auto-save（debounced PATCH to backend）+ 对模板模块做一次完整功能验收扫尾。

**Pre-state:**
- 后端 `PATCH /api/templates/:id` 已存在（iter 20 T2）
- 前端 designer store 已有 `dirty / snapshot` 内部机制 + localStorage 持久化
- TemplatesView / DesignerView inline 集成已就绪（iter 20）

---

## File Structure

| 文件 | 任务 |
|---|---|
| `apps/web/src/stores/designer.ts` | T1 |
| `apps/web/src/views/DesignerView.vue` | T2 |
| `apps/web/src/designer/DesignerHeader.vue` | T3 |
| `apps/web/src/designer/TemplateNameEditor.vue` | T2 |
| `docs/qa/template-module-verification-checklist.md` (new) | T5 |
| — | T4 (beforeunload) integrates into T3 |
| — | T6 验收 |

---

### Task 1: designer store — auto-save 核心机制

**File:** `apps/web/src/stores/designer.ts`

- [ ] **Step 1: 在 state 加 templateId + saveStatus 字段**

  在 `state: () => ({ ... })` 内部追加：
  ```ts
  templateId: null as string | null,
  saveStatus: 'idle' as 'idle' | 'pending' | 'saving' | 'saved' | 'error',
  lastSavedAt: null as number | null,
  saveError: null as string | null,
  ```

- [ ] **Step 2: 加入 auto-save action**

  在 actions 区域追加（位置：放在 `commit()` 旁边）：
  ```ts
  setTemplateId(id: string | null): void {
    this.templateId = id;
    this.saveStatus = 'idle';
    this.saveError = null;
  },

  // Internal — called by the debounced wrapper in DesignerView
  async saveToBackend(): Promise<void> {
    if (!this.templateId) return;
    this.saveStatus = 'saving';
    this.saveError = null;
    try {
      const { apiFetch } = await import('../lib/api');
      await apiFetch<{ id: string }>(`/templates/${this.templateId}`, {
        method: 'PATCH',
        body: JSON.stringify({
          name: this.template.meta.name,
          data: this.template,
        }),
      });
      this.saveStatus = 'saved';
      this.lastSavedAt = Date.now();
      this.dirty = false;
    } catch (e) {
      this.saveStatus = 'error';
      this.saveError = (e as Error).message ?? '保存失败';
    }
  },

  markPendingSave(): void {
    if (this.templateId && this.saveStatus !== 'saving') {
      this.saveStatus = 'pending';
    }
  },
  ```

- [ ] **Step 3: 在 loadTemplate 内重置 saveStatus 并写 templateId**

  Find `loadTemplate` action（如果不存在，找类似的「load from data」入口）。在末尾追加：
  ```ts
  this.saveStatus = 'idle';
  this.saveError = null;
  this.lastSavedAt = null;
  ```

- [ ] **Step 4: 类型检查 + commit**

  ```bash
  docker compose -f docker-compose.dev.yml exec -T web sh -c 'cd /workspace/apps/web && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/vue-tsc --noEmit'
  git add apps/web/src/stores/designer.ts
  git commit -m "feat(store): designer store 加 templateId / saveStatus + saveToBackend / markPendingSave"
  ```

---

### Task 2: DesignerView — 绑定 templateId + debounced auto-save watcher

**File:** `apps/web/src/views/DesignerView.vue`

- [ ] **Step 1: 在 loadById 成功后 setTemplateId**

  Find:
  ```ts
  store.loadTemplate(data);
  ```
  在所有 `store.loadTemplate(data)` 调用后插入：
  ```ts
  store.loadTemplate(data);
  store.setTemplateId(id);
  ```

  并且如果存在 `store.reset()` 的 fallback 分支（loadById catch 或 initialize 内），加：
  ```ts
  store.reset();
  store.setTemplateId(null);  // unbound from any persistent template
  ```

- [ ] **Step 2: 在 `<script setup>` 加 debounced watcher**

  在 `onMounted` 之前加入：
  ```ts
  import { watch } from 'vue';   // 确保 import 在

  const SAVE_DEBOUNCE_MS = 1500;
  let saveTimer: ReturnType<typeof setTimeout> | null = null;

  watch(
    () => store.template,
    () => {
      // Only auto-save when we have a backing template id
      if (!store.templateId) return;
      store.markPendingSave();
      if (saveTimer) clearTimeout(saveTimer);
      saveTimer = setTimeout(() => {
        void store.saveToBackend();
        saveTimer = null;
      }, SAVE_DEBOUNCE_MS);
    },
    { deep: true },
  );
  ```

  注意：`watch deep: true` 监听整个 template，包含 anchor 拖动每一帧的变化。为了避免频繁触发，debounce 已经够（1.5s 内的连续变化合并为一次）。

- [ ] **Step 3: beforeunload 防止丢失未保存改动**

  在 `<script setup>` 加：
  ```ts
  import { onMounted, onBeforeUnmount, watch } from 'vue';

  function onBeforeUnload(e: BeforeUnloadEvent): void {
    if (store.saveStatus === 'pending' || store.saveStatus === 'saving' || store.dirty) {
      e.preventDefault();
      e.returnValue = '';
    }
  }

  onMounted(() => {
    void initialize();
    window.addEventListener('beforeunload', onBeforeUnload);
  });
  onBeforeUnmount(() => {
    if (saveTimer) {
      clearTimeout(saveTimer);
      saveTimer = null;
      // Fire one last save before unmounting to flush pending changes
      void store.saveToBackend();
    }
    window.removeEventListener('beforeunload', onBeforeUnload);
  });
  ```

  注：替换原来 `onMounted(() => { void initialize(); });` 为上面合并版本。

- [ ] **Step 4: 类型检查 + commit**

  ```bash
  docker compose -f docker-compose.dev.yml exec -T web sh -c 'cd /workspace/apps/web && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/vue-tsc --noEmit'
  git add apps/web/src/views/DesignerView.vue
  git commit -m "feat(designer): DesignerView debounced auto-save + beforeunload 阻止丢失改动"
  ```

---

### Task 3: DesignerHeader — 保存状态指示器

**File:** `apps/web/src/designer/DesignerHeader.vue`

- [ ] **Step 1: 加 computed 显示文案**

  在 `<script setup>` 加：
  ```ts
  import { CloudOff } from 'lucide-vue-next';  // 加到现有 lucide imports 内

  const saveStatusText = computed(() => {
    if (!store.templateId) return ''; // un-backed template — no indicator
    if (store.saveStatus === 'saving') return '保存中…';
    if (store.saveStatus === 'pending') return '改动未保存';
    if (store.saveStatus === 'error') return `⚠ 保存失败 · 点击重试`;
    if (store.saveStatus === 'saved' && store.lastSavedAt) {
      const sec = Math.floor((Date.now() - store.lastSavedAt) / 1000);
      if (sec < 5) return '✓ 已保存';
      if (sec < 60) return `✓ 已保存 · ${sec}s 前`;
      const min = Math.floor(sec / 60);
      return `✓ 已保存 · ${min}m 前`;
    }
    return '';
  });

  const saveStatusColor = computed(() => {
    if (store.saveStatus === 'error') return '#d94f4f';
    if (store.saveStatus === 'saving' || store.saveStatus === 'pending') return '#888';
    return '#5a9b6a';
  });

  function retrySave(): void {
    if (store.saveStatus === 'error') void store.saveToBackend();
  }

  // tick so the "X 秒前" text refreshes
  const tickNow = ref(Date.now());
  let tickTimer: ReturnType<typeof setInterval> | null = null;
  onMounted(() => {
    tickTimer = setInterval(() => { tickNow.value = Date.now(); }, 5000);
  });
  onBeforeUnmount(() => {
    if (tickTimer) clearInterval(tickTimer);
  });
  ```

  注意 `import { ref, onMounted, onBeforeUnmount }` 等已经存在，调整即可。

  把 `saveStatusText` 里依赖 `Date.now()` 改为 `tickNow.value` 才会随 interval 重算：
  ```ts
  const saveStatusText = computed(() => {
    void tickNow.value; // dep
    // ... 原逻辑
  });
  ```

- [ ] **Step 2: 在 toolbar template 加显示元素**

  找一个合适位置（比如 `<span class="tt-spacer" />` 旁边或 toolbar 末尾），插入：
  ```vue
  <span
    v-if="saveStatusText"
    class="tt-save-status"
    :style="{ color: saveStatusColor, cursor: store.saveStatus === 'error' ? 'pointer' : 'default' }"
    @click="retrySave"
  >
    {{ saveStatusText }}
  </span>
  ```

  CSS：
  ```css
  .tt-save-status {
    font-size: 12px;
    margin: 0 12px;
    white-space: nowrap;
    user-select: none;
  }
  ```

- [ ] **Step 3: 类型检查 + commit**

  ```bash
  docker compose -f docker-compose.dev.yml exec -T web sh -c 'cd /workspace/apps/web && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/vue-tsc --noEmit'
  git add apps/web/src/designer/DesignerHeader.vue
  git commit -m "feat(designer): toolbar 保存状态指示器（保存中/已保存/失败重试）"
  ```

---

### Task 4: TemplateNameEditor — 改名同步触发 save

**File:** `apps/web/src/designer/TemplateNameEditor.vue`

- [ ] **Step 1: 阅读现有结构**

  Read the file. It should be calling `store.setName(name)` or similar when name changes.

- [ ] **Step 2: 确认改名走 store mutation**

  store.setName 改 `template.meta.name`，触发 DesignerView 的 deep watcher → debounced save。所以**通常不需要额外改动**。

  唯一需要注意：如果 setName 的实现是直接 `this.template.meta.name = name` 而不是 deep reactive trigger，确保 Vue 能侦测到。

  典型实现没问题。**这一步主要是验证而非改动**。

- [ ] **Step 3: 如果需要改动**

  无改动 → 跳到 T5。

  有改动 → commit。

---

### Task 5: 模板模块完整功能验收清单

**File:** Create `docs/qa/template-module-verification-checklist.md`

**Step 1**：先由 subagent / 我自己跑能自动化校验的部分。

**Step 2**：清单写完文件，提交。

**Step 3**：用户走查 UI / UX 部分。

---

### Task 6: 验收清单中发现的 bug 修复（动态）

走查过程中收集 → 优先级排序 → 修小 bug。

---

## 不在范围

- 不重做后端 PATCH endpoint（已存在）
- 不做 conflict resolution（多端编辑同一模板）— 后续再说
- 不做版本历史 / undo via backend — 走 store 内 history
- 不做 PR
