# Iter 20 — Auth Bug Fixes + Templates Inline Editor Plan

> **For agentic workers:** Use superpowers:subagent-driven-development.

**Goal:**
1. **Bug 1 4 项修复**：路由 guard hydrate 单次锁、auth.user null 时强制跳 /login、lark 前 logout、bfcache 处理
2. **模板中心 inline 编辑（方案 B 面包屑切换）**：
   - 后端 Templates CRUD
   - TemplatesView 卡片列表 + 进入编辑模式（同一路由内）
   - 编辑器和卡片之间用 View Transitions API 实现形变动画

**Tech Stack:** Nest + Prisma + Vue 3 + Pinia + ElementPlus + View Transitions API（带降级）

类型检查：
```bash
docker compose -f docker-compose.dev.yml exec -T web sh -c 'cd /workspace/apps/web && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/vue-tsc --noEmit'
docker compose -f docker-compose.dev.yml exec -T api sh -c 'cd /workspace/apps/api && npx tsc --noEmit'
```

---

## File Structure

| 文件 | 任务 |
|---|---|
| `apps/web/src/router/index.ts` | T1 |
| `apps/web/src/layout/AppShell.vue` | T1 |
| `apps/web/src/views/LoginView.vue` | T1 |
| `apps/api/prisma/schema.prisma` | T2 |
| `apps/api/prisma/migrations/...` (new) | T2 |
| `apps/api/src/templates/templates.controller.ts` (new) | T2 |
| `apps/api/src/templates/templates.service.ts` (new) | T2 |
| `apps/api/src/templates/templates.module.ts` (new) | T2 |
| `apps/api/src/app.module.ts` | T2 |
| `apps/web/src/stores/templates.ts` (new) | T3 |
| `apps/web/src/views/TemplatesView.vue` | T3, T4, T6 |
| `apps/web/src/views/DesignerView.vue` | T5 |
| — | T7 (整理/清理) |
| — | T8 (验收) |

---

### Task 1: Bug 1 四件套修复

**Files:** `apps/web/src/router/index.ts`, `apps/web/src/layout/AppShell.vue`, `apps/web/src/views/LoginView.vue`

#### 1A: 去掉 `hydrated` 单次锁

Find:
```ts
let hydrated = false;

router.beforeEach(async (to) => {
  const auth = useAuthStore();
  if (!hydrated) {
    await auth.hydrate();
    hydrated = true;
  }
  if (to.meta.requiresAuth && !auth.isAuthenticated) {
    return { path: '/login', query: { continue: to.fullPath } };
  }
  if (to.path === '/login' && auth.isAuthenticated) {
    return { path: '/templates' };
  }
  if (to.meta.adminOnly && auth.user?.role !== 'admin') {
    return { path: '/403' };
  }
  return true;
});
```
Replace with:
```ts
let hasHydratedOnce = false;

router.beforeEach(async (to) => {
  const auth = useAuthStore();
  // First-time hydrate on app boot (always).
  // Re-hydrate when entering a protected route with unknown auth (catches
  // bfcache restore where module state persists but session may have changed).
  const shouldHydrate = !hasHydratedOnce || (to.meta.requiresAuth && !auth.isAuthenticated);
  if (shouldHydrate) {
    await auth.hydrate();
    hasHydratedOnce = true;
  }
  if (to.meta.requiresAuth && !auth.isAuthenticated) {
    return { path: '/login', query: { continue: to.fullPath } };
  }
  if (to.path === '/login' && auth.isAuthenticated) {
    return { path: '/templates' };
  }
  if (to.meta.adminOnly && auth.user?.role !== 'admin') {
    return { path: '/403' };
  }
  return true;
});
```

#### 1B: AppShell 加全局 watcher — auth.user 变 null 时强制跳 /login

- [ ] Modify `apps/web/src/layout/AppShell.vue` script:

Find:
```ts
<script setup lang="ts">
import { ref } from 'vue';
import { useRoute } from 'vue-router';
import { computed } from 'vue';

import AppSidebar from './AppSidebar.vue';
import MustChangePasswordDialog from '../components/MustChangePasswordDialog.vue';

const route = useRoute();
const isFullscreen = computed(() => Boolean(route.meta.fullscreen));

const collapsed = ref(localStorage.getItem('tp_sidebar_collapsed') === 'true');
</script>
```

Replace with:
```ts
<script setup lang="ts">
import { ref, computed, watch, onMounted, onBeforeUnmount } from 'vue';
import { useRoute, useRouter } from 'vue-router';

import AppSidebar from './AppSidebar.vue';
import MustChangePasswordDialog from '../components/MustChangePasswordDialog.vue';
import { useAuthStore } from '../stores/auth';

const route = useRoute();
const router = useRouter();
const auth = useAuthStore();
const isFullscreen = computed(() => Boolean(route.meta.fullscreen));
const collapsed = ref(localStorage.getItem('tp_sidebar_collapsed') === 'true');

// Whenever auth becomes null while on a protected route, evict to /login.
// Covers: session expiry, logout from another tab, refresh-token failure.
watch(
  () => auth.user,
  (u, prev) => {
    if (prev && !u && route.meta.requiresAuth) {
      void router.push({ path: '/login', query: { continue: route.fullPath } });
    }
  },
);

// bfcache: when the page is restored from back-forward cache, force re-hydrate
// so we don't trust stale module-level state.
function onPageShow(ev: PageTransitionEvent): void {
  if (ev.persisted) {
    void auth.hydrate();
  }
}
onMounted(() => window.addEventListener('pageshow', onPageShow));
onBeforeUnmount(() => window.removeEventListener('pageshow', onPageShow));
</script>
```

#### 1C: LoginView 点飞书前先 logout()

Find:
```ts
function goLark(): void {
  const continueTo = (router.currentRoute.value.query.continue as string | undefined) ?? '/';
  window.location.assign(buildLarkLoginUrl(continueTo));
}
```
Replace with:
```ts
async function goLark(): Promise<void> {
  const continueTo = (router.currentRoute.value.query.continue as string | undefined) ?? '/';
  // Clear any stale session before redirecting to Lark — otherwise canceling
  // the Lark flow and pressing back leaves the user "logged in" with an
  // unexpected account.
  try {
    await authStore.logout();
  } catch {
    // ignore — logout endpoint may 401 if already logged out
  }
  window.location.assign(buildLarkLoginUrl(continueTo));
}
```

#### 1D: 类型检查 + commit

```bash
docker compose -f docker-compose.dev.yml exec -T web sh -c 'cd /workspace/apps/web && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/vue-tsc --noEmit'
git add apps/web/src/router/index.ts apps/web/src/layout/AppShell.vue apps/web/src/views/LoginView.vue
git commit -m "fix(auth): 四件套 — hydrate 重试 / auth.user 失效跳 /login / 飞书前 logout / bfcache"
```

---

### Task 2: 后端 Templates CRUD

**Files:** Prisma schema + migration + new templates module

- [ ] **Step 1: Prisma schema 加 Template model**

  Open `apps/api/prisma/schema.prisma`. In the model section (after `User` model), append:

  ```prisma
  model Template {
    id          String   @id @default(uuid())
    name        String
    description String?
    data        Json     // 整个 Template JSON（包含 canvas / elements / schema）
    ownerId     String   @map("owner_id")
    owner       User     @relation(fields: [ownerId], references: [id], onDelete: Cascade)
    createdAt   DateTime @default(now()) @map("created_at")
    updatedAt   DateTime @updatedAt @map("updated_at")
    @@index([ownerId, updatedAt(sort: Desc)])
    @@map("templates")
  }
  ```

  在 `User` model 内加反向关系：
  ```prisma
  // 在 User model 的字段列表中添加
  templates Template[]
  ```

- [ ] **Step 2: 生成 migration**

  ```bash
  docker compose -f docker-compose.dev.yml exec -T api sh -c 'cd /workspace/apps/api && npx prisma migrate dev --name add_templates'
  ```

- [ ] **Step 3: 创建 templates module**

  Create `apps/api/src/templates/templates.service.ts`:
  ```ts
  // eslint-disable-next-line import/no-unresolved
  import { Injectable, NotFoundException } from '@nestjs/common';
  // eslint-disable-next-line import/no-unresolved
  import { PrismaClient } from '@prisma/client';

  @Injectable()
  export class TemplatesService {
    constructor(private readonly prisma: PrismaClient) {}

    async list(ownerId: string) {
      return this.prisma.template.findMany({
        where: { ownerId },
        orderBy: { updatedAt: 'desc' },
        select: { id: true, name: true, description: true, createdAt: true, updatedAt: true },
      });
    }

    async get(ownerId: string, id: string) {
      const tpl = await this.prisma.template.findFirst({ where: { id, ownerId } });
      if (!tpl) throw new NotFoundException('template_not_found');
      return tpl;
    }

    async create(ownerId: string, payload: { name: string; description?: string; data: unknown }) {
      return this.prisma.template.create({
        data: {
          name: payload.name,
          description: payload.description ?? null,
          data: payload.data as object,
          ownerId,
        },
      });
    }

    async update(ownerId: string, id: string, payload: { name?: string; description?: string; data?: unknown }) {
      await this.get(ownerId, id); // ownership check
      return this.prisma.template.update({
        where: { id },
        data: {
          ...(payload.name !== undefined && { name: payload.name }),
          ...(payload.description !== undefined && { description: payload.description }),
          ...(payload.data !== undefined && { data: payload.data as object }),
        },
      });
    }

    async remove(ownerId: string, id: string): Promise<void> {
      await this.get(ownerId, id); // ownership check
      await this.prisma.template.delete({ where: { id } });
    }
  }
  ```

- [ ] **Step 4: Controller**

  Create `apps/api/src/templates/templates.controller.ts`:
  ```ts
  // eslint-disable-next-line import/no-unresolved
  import {
    Body,
    Controller,
    Delete,
    Get,
    Param,
    Patch,
    Post,
    BadRequestException,
  } from '@nestjs/common';
  // eslint-disable-next-line import/no-unresolved
  import { z } from 'zod';

  // eslint-disable-next-line import/no-unresolved
  import { CurrentUser } from '../auth/decorators/current-user.decorator.js';
  // eslint-disable-next-line import/no-unresolved
  import { TemplatesService } from './templates.service.js';

  const CreateDto = z.object({
    name: z.string().min(1).max(120),
    description: z.string().max(500).optional(),
    data: z.unknown(),
  });

  const UpdateDto = z.object({
    name: z.string().min(1).max(120).optional(),
    description: z.string().max(500).optional(),
    data: z.unknown().optional(),
  });

  @Controller('templates')
  export class TemplatesController {
    constructor(private readonly svc: TemplatesService) {}

    @Get()
    async list(@CurrentUser() me: { id: string }) {
      return this.svc.list(me.id);
    }

    @Get(':id')
    async get(@CurrentUser() me: { id: string }, @Param('id') id: string) {
      return this.svc.get(me.id, id);
    }

    @Post()
    async create(@CurrentUser() me: { id: string }, @Body() rawBody: unknown) {
      const parsed = CreateDto.safeParse(rawBody);
      if (!parsed.success) throw new BadRequestException(parsed.error.flatten());
      return this.svc.create(me.id, parsed.data);
    }

    @Patch(':id')
    async update(
      @CurrentUser() me: { id: string },
      @Param('id') id: string,
      @Body() rawBody: unknown,
    ) {
      const parsed = UpdateDto.safeParse(rawBody);
      if (!parsed.success) throw new BadRequestException(parsed.error.flatten());
      return this.svc.update(me.id, id, parsed.data);
    }

    @Delete(':id')
    async remove(@CurrentUser() me: { id: string }, @Param('id') id: string): Promise<{ ok: true }> {
      await this.svc.remove(me.id, id);
      return { ok: true };
    }
  }
  ```

- [ ] **Step 5: Module**

  Create `apps/api/src/templates/templates.module.ts`:
  ```ts
  // eslint-disable-next-line import/no-unresolved
  import { Module } from '@nestjs/common';

  // eslint-disable-next-line import/no-unresolved
  import { TemplatesController } from './templates.controller.js';
  // eslint-disable-next-line import/no-unresolved
  import { TemplatesService } from './templates.service.js';

  @Module({
    controllers: [TemplatesController],
    providers: [TemplatesService],
  })
  export class TemplatesModule {}
  ```

- [ ] **Step 6: 注册到 AppModule**

  在 `apps/api/src/app.module.ts` 的 `imports` 数组加：
  ```ts
  import { TemplatesModule } from './templates/templates.module.js';
  // ... imports: [..., TemplatesModule]
  ```

- [ ] **Step 7: 类型检查 + commit**

  ```bash
  docker compose -f docker-compose.dev.yml exec -T api sh -c 'cd /workspace/apps/api && npx tsc --noEmit'
  git add apps/api/prisma/schema.prisma apps/api/prisma/migrations apps/api/src/templates apps/api/src/app.module.ts
  git commit -m "feat(api): Templates CRUD — model + migration + GET/POST/PATCH/DELETE 端点"
  ```

---

### Task 3: 前端 templates store + TemplatesView 列表态

**Files:** Create `apps/web/src/stores/templates.ts`, modify `apps/web/src/views/TemplatesView.vue`

- [ ] **Step 1: Pinia store**

  Create `apps/web/src/stores/templates.ts`:
  ```ts
  // eslint-disable-next-line import/no-unresolved
  import { defineStore } from 'pinia';

  import { apiFetch } from '../lib/api';

  export interface TemplateListItem {
    id: string;
    name: string;
    description: string | null;
    createdAt: string;
    updatedAt: string;
  }

  export const useTemplatesStore = defineStore('templates', {
    state: () => ({
      list: [] as TemplateListItem[],
      loading: false,
    }),
    actions: {
      async fetchList(): Promise<void> {
        this.loading = true;
        try {
          const items = await apiFetch<TemplateListItem[]>('/templates');
          this.list = items;
        } finally {
          this.loading = false;
        }
      },
      async create(name: string, data: unknown): Promise<TemplateListItem> {
        const created = await apiFetch<TemplateListItem>('/templates', {
          method: 'POST',
          body: JSON.stringify({ name, data }),
        });
        this.list.unshift(created);
        return created;
      },
      async remove(id: string): Promise<void> {
        await apiFetch<{ ok: true }>(`/templates/${id}`, { method: 'DELETE' });
        this.list = this.list.filter((t) => t.id !== id);
      },
    },
  });
  ```

- [ ] **Step 2: TemplatesView 列表态（卡片网格）**

  完全重写 `apps/web/src/views/TemplatesView.vue` 现在只先做列表态，编辑器集成放在 T4：

  ```vue
  <script setup lang="ts">
  // eslint-disable-next-line import/no-unresolved
  import { ElButton, ElEmpty, ElMessage, ElMessageBox } from 'element-plus';
  // eslint-disable-next-line import/no-unresolved
  import { Plus, FileText, Trash2 } from 'lucide-vue-next';
  import { computed, onMounted, ref } from 'vue';

  import { useTemplatesStore } from '../stores/templates';

  const templates = useTemplatesStore();

  // Mode state — list vs editor. Editor integration lands in T4.
  type Mode = 'list' | 'editor';
  const mode = ref<Mode>('list');
  const currentId = ref<string | null>(null);

  onMounted(async () => {
    await templates.fetchList();
  });

  function openTemplate(id: string): void {
    currentId.value = id;
    mode.value = 'editor';
  }

  async function createNew(): Promise<void> {
    // Minimum default data — DesignerView will populate when mounted.
    const defaultData = { canvas: { paper: 'A4' }, elements: [] };
    try {
      const tpl = await templates.create('未命名模板', defaultData);
      openTemplate(tpl.id);
    } catch (e) {
      ElMessage.error('创建失败');
    }
  }

  async function deleteTemplate(id: string, name: string): Promise<void> {
    try {
      await ElMessageBox.confirm(`删除模板「${name}」？此操作不可恢复。`, '删除模板', {
        type: 'warning',
        confirmButtonText: '删除',
        cancelButtonText: '取消',
      });
    } catch {
      return;
    }
    try {
      await templates.remove(id);
      ElMessage.success('已删除');
    } catch {
      ElMessage.error('删除失败');
    }
  }

  function formatTime(iso: string): string {
    const d = new Date(iso);
    return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
  }
  </script>

  <template>
    <div class="tv-wrap">
      <!-- List mode -->
      <div v-if="mode === 'list'" class="tv-list">
        <header class="tv-head">
          <h1 class="tv-title">模板中心</h1>
          <ElButton type="primary" size="large" @click="createNew">
            <Plus :size="14" :stroke-width="2" style="margin-right: 6px" />
            新建模板
          </ElButton>
        </header>

        <div v-if="templates.loading" class="tv-loading">加载中…</div>
        <ElEmpty
          v-else-if="templates.list.length === 0"
          description="还没有模板 — 点「新建模板」开始"
        />
        <div v-else class="tv-grid">
          <div
            v-for="t in templates.list"
            :key="t.id"
            class="tv-card"
            :style="{ viewTransitionName: `tpl-card-${t.id}` }"
            @click="openTemplate(t.id)"
          >
            <div class="tv-card-thumb">
              <FileText :size="32" :stroke-width="1.5" />
            </div>
            <div class="tv-card-body">
              <div class="tv-card-name">{{ t.name }}</div>
              <div class="tv-card-time">{{ formatTime(t.updatedAt) }}</div>
            </div>
            <button
              class="tv-card-del"
              title="删除模板"
              @click.stop="deleteTemplate(t.id, t.name)"
            >
              <Trash2 :size="14" :stroke-width="2" />
            </button>
          </div>
        </div>
      </div>

      <!-- Editor mode placeholder — T4 fills in -->
      <div v-else class="tv-editor-placeholder">
        <ElButton @click="mode = 'list'; currentId = null">← 返回列表</ElButton>
        <p>编辑器占位（T4 集成）：模板 id = {{ currentId }}</p>
      </div>
    </div>
  </template>

  <style scoped>
  .tv-wrap { padding: 32px 40px; max-width: 1400px; margin: 0 auto; }
  .tv-head { display: flex; align-items: center; justify-content: space-between; margin-bottom: 24px; }
  .tv-title { font-size: 24px; font-weight: 600; color: var(--tp-ink, #1f1f23); margin: 0; }
  .tv-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 16px;
  }
  .tv-card {
    position: relative;
    background: #fff;
    border: 1px solid var(--tp-line, #ececef);
    border-radius: 12px;
    padding: 20px;
    cursor: pointer;
    transition: all 200ms cubic-bezier(0.4, 0, 0.2, 1);
  }
  .tv-card:hover {
    border-color: var(--tp-accent, #6c5ce7);
    box-shadow: 0 8px 24px rgba(108, 92, 231, 0.12);
    transform: translateY(-2px);
  }
  .tv-card-thumb {
    height: 80px;
    background: var(--tp-accent-bg, #f0eeff);
    border-radius: 8px;
    margin-bottom: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--tp-accent, #6c5ce7);
  }
  .tv-card-name { font-size: 14px; font-weight: 600; color: var(--tp-ink, #1f1f23); margin-bottom: 4px; }
  .tv-card-time { font-size: 11px; color: var(--tp-ink-faint, #9c9ca3); }
  .tv-card-del {
    position: absolute; top: 8px; right: 8px;
    width: 26px; height: 26px;
    border: none; background: transparent;
    border-radius: 6px; cursor: pointer;
    color: var(--tp-ink-faint, #9c9ca3);
    opacity: 0;
    transition: all 120ms ease;
  }
  .tv-card:hover .tv-card-del { opacity: 1; }
  .tv-card-del:hover { background: #fee; color: #d94f4f; }
  .tv-loading { padding: 60px 0; text-align: center; color: var(--tp-ink-faint, #9c9ca3); }
  .tv-editor-placeholder { padding: 60px; text-align: center; }
  </style>
  ```

- [ ] **Step 3: 类型检查 + commit**

  ```bash
  docker compose -f docker-compose.dev.yml exec -T web sh -c 'cd /workspace/apps/web && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/vue-tsc --noEmit'
  git add apps/web/src/stores/templates.ts apps/web/src/views/TemplatesView.vue
  git commit -m "feat(templates): templates store + 卡片网格列表 + 创建/删除 + 卡片 view-transition-name"
  ```

---

### Task 4 & 5: DesignerView 解耦 + 集成到 TemplatesView 编辑模式

**Goal:** DesignerView 改为接收 `templateId` prop（不再依赖 route param），然后在 TemplatesView 内 inline 渲染。

> 这里改动较大且耦合 — 一个 task 内做完。

- [ ] **Step 1: 阅读 DesignerView.vue 当前结构**

  Read `apps/web/src/views/DesignerView.vue` to understand how it currently loads template data (from route param? from localStorage? from API?).

- [ ] **Step 2: 把 DesignerView 改为支持 props.templateId**

  在 DesignerView script 内加：
  ```ts
  const props = defineProps<{
    templateId?: string;  // optional — undefined means "new template"
    embedded?: boolean;   // true when used inline within TemplatesView, hides own top toolbar / fullscreen affordances
  }>();
  ```

  把 route 解析的代码改为读 `props.templateId`：
  - 如果 `props.templateId` 存在 → 从 store/API 拉对应模板
  - 否则 → 用 default template

  保留 route-based 入口作为兼容（直接访问 /designer/:id 仍可工作），通过 `useRoute().params.id` 注入到 props 的初始值。

- [ ] **Step 3: TemplatesView 集成 DesignerView**

  在 TemplatesView.vue script 加：
  ```ts
  import DesignerView from './DesignerView.vue';
  ```

  替换 placeholder template 块：
  Find:
  ```vue
  <div v-else class="tv-editor-placeholder">
    <ElButton @click="mode = 'list'; currentId = null">← 返回列表</ElButton>
    <p>编辑器占位（T4 集成）：模板 id = {{ currentId }}</p>
  </div>
  ```
  Replace with:
  ```vue
  <div v-else class="tv-editor-mode">
    <header class="tv-breadcrumb">
      <button class="tv-back" @click="returnToList">
        ← 返回模板中心
      </button>
      <span class="tv-bc-sep">/</span>
      <span class="tv-bc-current">{{ currentTemplateName }}</span>
    </header>
    <div class="tv-editor-host">
      <DesignerView v-if="currentId" :template-id="currentId" :embedded="true" />
    </div>
  </div>
  ```

  并在 script 加：
  ```ts
  const currentTemplateName = computed(() => {
    if (!currentId.value) return '';
    const t = templates.list.find((x) => x.id === currentId.value);
    return t?.name ?? '未命名';
  });

  async function returnToList(): Promise<void> {
    await transitionTo('list');
  }

  // Wrap mode transitions with View Transitions API where available.
  async function transitionTo(target: 'list' | 'editor', id?: string): Promise<void> {
    const doSwitch = (): void => {
      if (target === 'editor' && id) currentId.value = id;
      mode.value = target;
      if (target === 'list') currentId.value = null;
    };
    type ViewTransitionAPI = Document & {
      startViewTransition?: (cb: () => void) => { finished: Promise<void> };
    };
    const doc = document as ViewTransitionAPI;
    if (typeof doc.startViewTransition === 'function') {
      doc.startViewTransition(doSwitch);
    } else {
      doSwitch();
    }
  }
  ```

  改 openTemplate / createNew 调用：
  ```ts
  function openTemplate(id: string): void {
    void transitionTo('editor', id);
  }
  async function createNew(): Promise<void> {
    const defaultData = { canvas: { paper: 'A4' }, elements: [] };
    try {
      const tpl = await templates.create('未命名模板', defaultData);
      void transitionTo('editor', tpl.id);
    } catch {
      ElMessage.error('创建失败');
    }
  }
  ```

  加 styles：
  ```css
  .tv-editor-mode { display: flex; flex-direction: column; height: 100%; }
  .tv-breadcrumb {
    padding: 12px 24px; background: #fff;
    border-bottom: 1px solid var(--tp-line, #ececef);
    display: flex; align-items: center; gap: 8px;
    font-size: 13px; color: var(--tp-ink-soft, #5e5e66);
  }
  .tv-back {
    background: transparent; border: none; cursor: pointer;
    color: var(--tp-accent, #6c5ce7); font-weight: 500; padding: 4px 8px;
    border-radius: 4px; transition: background 120ms ease;
  }
  .tv-back:hover { background: var(--tp-accent-bg, #f0eeff); }
  .tv-bc-sep { color: var(--tp-ink-faint, #9c9ca3); }
  .tv-bc-current { color: var(--tp-ink, #1f1f23); font-weight: 500; }
  .tv-editor-host { flex: 1; min-height: 0; }
  ```

- [ ] **Step 4: 调整 TemplatesView.vue wrap CSS**

  Find:
  ```css
  .tv-wrap { padding: 32px 40px; max-width: 1400px; margin: 0 auto; }
  ```
  Replace with:
  ```css
  .tv-wrap {
    padding: 32px 40px;
    max-width: 1400px;
    margin: 0 auto;
    min-height: 100%;
  }
  .tv-list { /* list mode keeps padding wrap */ }
  /* Editor mode breaks out of max-width to fill */
  .tv-wrap:has(.tv-editor-mode) {
    max-width: none;
    padding: 0;
  }
  ```

- [ ] **Step 5: 类型检查 + commit**

  ```bash
  docker compose -f docker-compose.dev.yml exec -T web sh -c 'cd /workspace/apps/web && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/vue-tsc --noEmit'
  git add apps/web/src/views/DesignerView.vue apps/web/src/views/TemplatesView.vue
  git commit -m "feat(templates): DesignerView 解耦支持 templateId prop + TemplatesView inline 集成 + View Transitions 形变切换"
  ```

---

### Task 6: View Transitions CSS — 形变收回动画

**File:** `apps/web/src/views/TemplatesView.vue` (style block additions)

- [ ] **Step 1: 给两个模式定义视图过渡名 + 动画曲线**

  在 TemplatesView.vue `<style scoped>` 末尾追加（仅生效于支持的浏览器）：

  ```css
  /* ===== View Transitions API — list ↔ editor 形变动画 ===== */

  /* Mark the editor host as the morph target */
  .tv-editor-mode {
    view-transition-name: tpl-editor-host;
  }

  /* Mark each card with a unique transition name (already done in v-bind :style="{ viewTransitionName: `tpl-card-${t.id}` }") */

  /* Custom timing for enter/exit */
  @keyframes tv-fade-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  @keyframes tv-fade-out {
    from { opacity: 1; }
    to { opacity: 0; }
  }
  @keyframes tv-scale-in {
    from { transform: scale(0.95); opacity: 0; }
    to { transform: scale(1); opacity: 1; }
  }
  @keyframes tv-scale-out {
    from { transform: scale(1); opacity: 1; }
    to { transform: scale(0.95); opacity: 0; }
  }
  </style>
  ```

  注意 `view-transition-name` 不能在 scoped CSS 里直接生效，因为 Vue 会把它 hash 化。改为 inline style 或 deep selector。

  **正确做法：** template 上用 `:style="{ viewTransitionName: '...' }"` 或在 global CSS 配置。

  在 TemplatesView template 中：
  Find:
  ```vue
  <div v-else class="tv-editor-mode">
  ```
  Replace with:
  ```vue
  <div v-else class="tv-editor-mode" :style="{ viewTransitionName: 'tpl-editor-host' }">
  ```

- [ ] **Step 2: 全局样式增加 transition 选择器（这里需要非 scoped 才生效）**

  Create or append to `apps/web/src/styles/transitions.css` (new file):
  ```css
  /* View Transitions API — 全局动画曲线 */
  ::view-transition-old(root),
  ::view-transition-new(root) {
    animation-duration: 280ms;
    animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Editor host morphs in/out with subtle scale */
  ::view-transition-old(tpl-editor-host) {
    animation-name: tv-editor-out;
    animation-duration: 320ms;
  }
  ::view-transition-new(tpl-editor-host) {
    animation-name: tv-editor-in;
    animation-duration: 320ms;
  }
  @keyframes tv-editor-out {
    from { opacity: 1; transform: scale(1); }
    to { opacity: 0; transform: scale(0.96); }
  }
  @keyframes tv-editor-in {
    from { opacity: 0; transform: scale(1.03); }
    to { opacity: 1; transform: scale(1); }
  }

  /* Cards morph individually — browser handles bbox transition automatically
     when both old and new have the same view-transition-name. */
  ::view-transition-old([class*='tpl-card-']),
  ::view-transition-new([class*='tpl-card-']) {
    animation-duration: 320ms;
    animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }
  ```

  在 `apps/web/src/main.ts` 顶部加 import：
  ```ts
  import './styles/transitions.css';
  ```

- [ ] **Step 3: 类型检查 + commit**

  ```bash
  docker compose -f docker-compose.dev.yml exec -T web sh -c 'cd /workspace/apps/web && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/vue-tsc --noEmit'
  git add apps/web/src/views/TemplatesView.vue apps/web/src/styles/transitions.css apps/web/src/main.ts
  git commit -m "feat(transitions): View Transitions API — 模板卡 / 编辑器形变切换 + 自定义曲线"
  ```

---

### Task 7: 路由清理 — /designer/* 保留但优先 inline

**File:** `apps/web/src/router/index.ts`

- [ ] **Step 1: 把 /designer/:id 改为 redirect 到 /templates，由 TemplatesView 接管 inline 打开**

  Find:
  ```ts
  {
    path: '/designer/new',
    name: 'designer-new',
    meta: { requiresAuth: true, fullscreen: true },
    component: () => import('../views/DesignerView.vue'),
  },
  {
    path: '/designer/:id',
    name: 'designer-edit',
    meta: { requiresAuth: true, fullscreen: true },
    component: () => import('../views/DesignerView.vue'),
  },
  ```

  Replace with:
  ```ts
  // Legacy direct designer URLs — redirect to templates with hash so TemplatesView opens that one inline
  {
    path: '/designer/new',
    redirect: '/templates?new=1',
  },
  {
    path: '/designer/:id',
    redirect: (to) => `/templates?open=${to.params.id}`,
  },
  ```

  并在 TemplatesView script onMounted 加 query 处理：
  ```ts
  import { useRoute, useRouter } from 'vue-router';
  const route = useRoute();
  const router = useRouter();

  onMounted(async () => {
    await templates.fetchList();
    if (route.query.new === '1') {
      void createNew();
      void router.replace({ query: {} });
    } else if (typeof route.query.open === 'string') {
      const id = route.query.open;
      openTemplate(id);
      void router.replace({ query: {} });
    }
  });
  ```

- [ ] **Step 2: 类型检查 + commit**

  ```bash
  docker compose -f docker-compose.dev.yml exec -T web sh -c 'cd /workspace/apps/web && NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/vue-tsc --noEmit'
  git add apps/web/src/router/index.ts apps/web/src/views/TemplatesView.vue
  git commit -m "feat(router): /designer/* legacy URL 重定向到 /templates inline 打开"
  ```

---

### Task 8: 最终验收

- [ ] **Step 1: vue-tsc 通过**
- [ ] **Step 2: api tsc 通过**
- [ ] **Step 3: schema 46/46 pass**
- [ ] **Step 4: 浏览器走查（Ctrl+Shift+R 硬刷新）**

  **Bug 1 修复验证：**
  - [ ] 登录后再登出 → 路由跳到 /login（不能停留在 /templates）
  - [ ] 在 /login 点「飞书登录」→ 跳 lark → 取消返回 → 应在 /login 看到登录表单（不该越权进 /templates）
  - [ ] 测试 bfcache: emergency admin 登录 → 主动 logout → 后退按钮 → 应跳 /login（不能恢复 /templates）

  **方案 B inline 编辑：**
  - [ ] /templates 默认显示卡片网格
  - [ ] 点「+ 新建模板」→ 编辑器以形变动画打开，新模板进入编辑
  - [ ] 点已有卡片 → 编辑器形变打开
  - [ ] 编辑器顶部面包屑显示「← 返回模板中心 / 模板名」
  - [ ] 点「← 返回模板中心」→ 编辑器以形变动画收回为卡片
  - [ ] 切换浏览器到 Chrome 113+（支持 View Transitions）→ 动画流畅
  - [ ] Firefox / Safari < 18：直接切换无动画也能工作（降级）
  - [ ] 访问 `/designer/new` 跳转到 /templates 并打开新建
  - [ ] 访问 `/designer/<existingId>` 跳转到 /templates 并打开该模板

  **完整性回归：**
  - [ ] 编辑器内拖动 / 缩放 / 预览 / 打印仍正常
  - [ ] 个人中心 / API 说明 / 用户管理跳转正常
  - [ ] sidebar 折叠 / 用户头像 / 退出按钮正常

- [ ] **Step 5: TaskUpdate iter 20 master task → completed**

---

## 不在范围

- 不实现 templates 缩略图（生成 / 缓存留给后续 iter）
- 不实现模板复制 / 重命名（卡片只有点击 / 删除）
- 不发起 PR
